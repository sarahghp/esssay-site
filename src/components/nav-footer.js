import React from 'react'

const NavFooter = ({ current }) => {

  return (
    <h4
      style={{
        alignSelf: 'flex-start',
        marginTop: '2rem'
      }}>
      <a href='/'>↑</a>&nbsp;
    </h4>
  )
}

export default NavFooter
